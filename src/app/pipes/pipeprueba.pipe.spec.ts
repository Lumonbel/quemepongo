import { PipepruebaPipe } from './pipeprueba.pipe';

describe('PipepruebaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipepruebaPipe();
    expect(pipe).toBeTruthy();
  });
});
